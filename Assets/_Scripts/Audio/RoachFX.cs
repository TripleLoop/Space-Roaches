using UnityEngine;
using System.Collections;

public class RoachFX : MonoBehaviour {
    
    private AudioSource _source;

    // Use this for initialization
    void Start()
    {
        _source.Play();
    }

    // Update is called once per frame
    void Update () {
	
	}
}
